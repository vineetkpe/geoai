In app/login/page.tsx and app/signup/page.tsx, comment out (don't delete) the "Continue with Google" button and its signInWithOAuth handler in both files. Leave a clear // TODO: re-enable when Google OAuth provider is configured in Supabase comment above each. Keep the email/password form fully working, don't touch anything else.